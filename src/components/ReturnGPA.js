import PropTypes from 'prop-types'
import Task from './Task'
import Tasks from './Tasks'

const ReturnGPA = (tasks) => {


  return (
     <header className="footer">
        <h1> Your GPA is :</h1> 
     </header>
  )
}

export default ReturnGPA